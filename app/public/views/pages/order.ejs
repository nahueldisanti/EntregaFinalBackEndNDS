<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
        <script src="https://code.iconify.design/iconify-icon/1.0.0-beta.3/iconify-icon.min.js"></script>
        <script src="/socket.io/socket.io.js"></script>
        <title>Orden Creada</title>
    </head>
    <body>
        <header>
            <%- include('../partials/navbar.ejs'); %>
        </header>
        <div class="container">
            <div class="alert alert-danger mt-4" role="alert">
                <h1> Tu Orden ha sido creada con exito !</h1>
                <section class="mt-6">
                    <div class="container is-flex is-justify-content-space-between is-flex-wrap-wrap" id="divList">
                        <% orderProducts.forEach(product=> { %>
                        <div class="card" style="width: 250px; margin: 30px">
                            <div class="card-image" style="padding: 15px">
                            </div>
                            <div class="card-content">
                                <div class="media">
                                    <div class="media-content">
                                        <p class="title is-4"><%=product.name %></p>
                                        <p class="subtitle is-6">$ <%=product.priceUnit %></p>

                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }); %>
            </div>
            <div class="d-grid gap-2 ">
                <a class="btn btn-danger" href="/auth/login">Volver al Inicio</a>
            </div>
        </div>
    </body>
</html>
